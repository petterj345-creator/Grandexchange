package com.github.petterj345.grandexchange.input;

/** What a player's next chat message is being interpreted as. */
public enum PromptType {
    BUY_QUANTITY,
    BUY_PRICE,
    SELL_QUANTITY,
    SELL_PRICE
}
